apiVersion: v1
kind: Service
metadata:
  name: central-suporte-backend-service
  namespace: central-suporte
spec:
  type: NodePort
  selector:
    app: central-suporte-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4000
