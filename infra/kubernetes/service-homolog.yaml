apiVersion: v1
kind: Service
metadata:
  name: central-suporte-backend-homolog-service
  namespace: central-suporte-homolog
spec:
  type: NodePort
  selector:
    app: central-suporte-backend-homolog
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4000
