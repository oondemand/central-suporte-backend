apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: app-tls
  namespace: central-suporte
spec:
  secretName: app-tls
  issuerRef:
    name: letsencrypt-nginx
    kind: ClusterIssuer
  dnsNames:
    - api.suporte.oondemand.online
    - central.oondemand.online
